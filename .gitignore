# Python envs — torch alone is ~750 MB, never commit these
.venv/
venv/
__pycache__/
*.pyc

# Node (frontend)
node_modules/
.amplify/
dist/
build/

# Generated test audio (patterns match at any depth). Regenerate with:
#   say -v Samantha -o audio/sample.wav --data-format=LEI16@22050 "..."
#   python test_rerecord.py prepare
#   python test_robustness.py
robustness/
id_*.wav
wm_*.wav
sample_wm.wav

# Local phone recordings from the re-record test
recording*
rerecorded_capture.wav
listen_*.wav
*.m4a

.DS_Store

# Local-only: internal project brief, and auto-generated agent tooling config
# (AWS Agent Toolkit regenerates these per machine — nothing project-specific)
context.md
AGENTS.md
CLAUDE.md
.cursor/
